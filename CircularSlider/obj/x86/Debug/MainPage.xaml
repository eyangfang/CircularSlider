<Page
    x:Class="CircularSlider.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CircularSlider"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:CircularSlider.UserControl"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="UserControl/Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <control:CircularSlider Style="{StaticResource Percentage}"
                              Value="60"
                              ScaleBrush="{StaticResource ScaleBrush}"
                              TrailBrush="{StaticResource TrailBrush}"
                              UnitBrush="{StaticResource TrailBrush}"
                              ValueBrush="{StaticResource TrailBrush}"
                              IsInteractive="True"
                              Margin="20 0 20 20"
                              Width="200"
                              Height="200"/>
    </Grid>
</Page>

